/** @type {import('tailwindcss').Config} */
module.exports = {
  // Usa 'selector' (para Tailwind v3.4+) o 'class' (para versiones anteriores)
  darkMode: 'selector',
  content: [
    "./src/**/*.{html,ts}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
